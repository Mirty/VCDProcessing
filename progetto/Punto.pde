class Punto {
  // proprietà 
  int x, y;
  
  // costruttori
  Punto (int x, int y) {
    this.x = x;
    this.y = y;
  }
  
  Punto (Punto p) {
    x = p.x;
    y = p.y;
  }
  
  // metodi e funzioni
}